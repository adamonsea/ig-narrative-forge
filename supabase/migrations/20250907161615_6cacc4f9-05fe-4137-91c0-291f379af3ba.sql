-- Move pg_trgm extension functions have proper search path 
-- Note: The extensions need to remain in public for compatibility
-- Just ensure proper configuration

-- These remaining warnings about extensions in public are acceptable
-- as pg_trgm and uuid-ossp commonly remain in public schema

-- Continue with implementation
SELECT 1;